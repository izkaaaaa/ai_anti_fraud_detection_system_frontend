# 三层防御机制对接文档

## 📋 概述

本系统采用**三层防御机制**，根据检测到的风险等级自动升级防御等级，为用户提供分级保护。

---

## 🛡️ 三层防御等级

### Level 1: 正常模式（绿色）🟢

**状态：** 安全

**触发条件：**
- 未检测到任何风险
- 检测结果为真实内容

**前端表现：**
- ✅ 正常通话界面
- ✅ 后台静默检测
- ✅ 不显示警告提示
- ✅ 绿色状态指示器

**后端行为：**
- 持续监测音频/视频/文本
- 记录检测日志
- 不发送短信通知

---

### Level 2: 警惕模式（黄色）🟡

**状态：** 中等风险

**触发条件：**
- 检测到AI合成语音
- 检测到AI换脸视频
- 文本匹配到中等风险敏感词
- 风险等级为 `medium` 或 `high`

**前端表现：**
- ⚠️ 显示警告弹窗（Modal）
- ⚠️ 黄色状态指示器
- ⚠️ 提示用户提高警惕
- ⚠️ 建议挂断通话按钮
- ⚠️ 可选：震动提醒

**后端行为：**
- 发送控制指令升级防御等级
- 提高检测频率（视频从1fps升到30fps）
- 发送短信通知家庭组成员
- 记录详细检测日志

---

### Level 3: 危险模式（红色）🔴

**状态：** 高危风险

**触发条件：**
- 同时检测到多种风险（音频+视频+文本）
- 触发高危敏感词（risk_level >= 4）
- 视频防抖确认为Deepfake（5次中3次为假）
- 风险等级为 `critical`

**前端表现：**
- 🚨 全屏警告（覆盖整个通话界面）
- 🚨 红色状态指示器
- 🚨 强烈建议立即挂断
- 🚨 震动+声音警报
- 🚨 自动开启通话录音（保存证据）
- 🚨 显示报警按钮（一键报警）

**后端行为：**
- 立即发送短信给家庭组所有成员
- 记录完整通话证据
- 可能触发自动挂断（根据配置）
- 推送到监控中心

---

## 📊 风险等级分类

后端会返回以下5个风险等级：

| 风险等级 | 英文标识 | 对应防御等级 | 是否发短信 | 前端展示方式 |
|---------|---------|-------------|-----------|------------|
| 安全 | `safe` | Level 1 | ❌ | 无提示 |
| 低危 | `low` | Level 1 | ❌ | Toast轻提示 |
| 中危 | `medium` | Level 2 | ✅ | Modal弹窗 |
| 高危 | `high` | Level 2 | ✅ | Modal弹窗 |
| 极危 | `critical` | Level 3 | ✅ | 全屏警告 |

---

## 📡 接收防御升级消息

### 消息格式

当后端检测到风险时，会通过WebSocket推送控制指令：

```json
{
  "type": "control",
  "action": "upgrade_level",
  "target_level": 2,
  "reason": "检测到AI合成语音",
  "config": {
    "video_fps": 30.0,
    "ui_action": "simulate_hangup",
    "ui_message": "⚠️ 检测到AI合成语音，请立即挂断！",
    "warning_mode": "modal",
    "show_full_screen_warning": false,
    "enable_call_recording": true,
    "block_call": false
  }
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `type` | string | 固定为 `"control"` |
| `action` | string | 动作类型，固定为 `"upgrade_level"` |
| `target_level` | int | 目标防御等级（1/2/3） |
| `reason` | string | 升级原因 |
| `config.video_fps` | float | 建议的视频帧率 |
| `config.ui_action` | string | UI动作（`simulate_hangup`/`show_warning`） |
| `config.ui_message` | string | 显示给用户的消息 |
| `config.warning_mode` | string | 警告模式（`modal`/`fullscreen`/`toast`） |
| `config.show_full_screen_warning` | bool | 是否显示全屏警告 |
| `config.enable_call_recording` | bool | 是否开启录音 |
| `config.block_call` | bool | 是否建议挂断 |

---

## 📥 接收检测结果消息

### 消息格式

```json
{
  "type": "info",
  "data": {
    "title": "⚠️ 检测到AI合成语音",
    "message": "置信度95%，请提高警惕",
    "risk_level": "high",
    "confidence": 0.95,
    "call_id": 123,
    "timestamp": "2026-02-23T10:00:00",
    "display_mode": "popup"
  }
}
```

### display_mode 说明

| 模式 | 说明 | 适用场景 |
|------|------|---------|
| `toast` | 轻量提示 | 低危/安全 |
| `popup` | 弹窗警告 | 中危/高危 |
| `fullscreen` | 全屏警告 | 极危 |

---

## 💻 前端实现示例

### 1. 处理控制指令

```javascript
class DefenseManager {
  constructor() {
    this.currentLevel = 1;  // 当前防御等级
  }
  
  // 处理防御升级消息
  handleControlMessage(message) {
    if (message.action === 'upgrade_level') {
      const targetLevel = message.target_level;
      const config = message.config;
      
      console.log(`🛡️ 防御升级: Level ${this.currentLevel} → Level ${targetLevel}`);
      console.log(`   原因: ${message.reason}`);
      
      // 更新当前等级
      this.currentLevel = targetLevel;
      
      // 根据等级执行不同操作
      switch (targetLevel) {
        case 1:
          this.applyLevel1(config);
          break;
        case 2:
          this.applyLevel2(config);
          break;
        case 3:
          this.applyLevel3(config);
          break;
      }
    }
  }
  
  // Level 1: 正常模式
  applyLevel1(config) {
    console.log('✅ 切换到正常模式');
    
    // UI更新
    this.updateStatusIndicator('green', '安全');
    this.hideAllWarnings();
    
    // 恢复正常检测频率
    this.setVideoFPS(1);
  }
  
  // Level 2: 警惕模式
  applyLevel2(config) {
    console.log('⚠️ 切换到警惕模式');
    
    // UI更新
    this.updateStatusIndicator('yellow', '警惕');
    
    // 显示警告弹窗
    this.showWarningModal({
      title: '⚠️ 风险警告',
      message: config.ui_message,
      buttons: [
        { text: '继续通话', style: 'secondary' },
        { text: '立即挂断', style: 'danger', action: () => this.hangup() }
      ]
    });
    
    // 提高检测频率
    if (config.video_fps) {
      this.setVideoFPS(config.video_fps);
    }
    
    // 开启录音
    if (config.enable_call_recording) {
      this.startRecording();
    }
    
    // 震动提醒
    if (navigator.vibrate) {
      navigator.vibrate([200, 100, 200]);
    }
  }
  
  // Level 3: 危险模式
  applyLevel3(config) {
    console.log('🚨 切换到危险模式');
    
    // UI更新
    this.updateStatusIndicator('red', '危险');
    
    // 显示全屏警告
    this.showFullScreenWarning({
      title: '🚨 高危风险警告',
      message: config.ui_message,
      subMessage: '系统检测到多重风险，强烈建议立即挂断！',
      buttons: [
        { text: '立即挂断', style: 'danger', action: () => this.hangup() },
        { text: '一键报警', style: 'primary', action: () => this.callPolice() }
      ]
    });
    
    // 强烈震动+声音警报
    if (navigator.vibrate) {
      navigator.vibrate([500, 200, 500, 200, 500]);
    }
    this.playAlertSound();
    
    // 自动开启录音
    this.startRecording();
    
    // 如果配置了自动挂断
    if (config.block_call) {
      setTimeout(() => {
        this.showAutoHangupCountdown(5);  // 5秒后自动挂断
      }, 3000);
    }
  }
  
  // 更新状态指示器
  updateStatusIndicator(color, text) {
    const indicator = document.querySelector('.defense-indicator');
    indicator.className = `defense-indicator ${color}`;
    indicator.textContent = text;
  }
  
  // 显示警告弹窗
  showWarningModal(options) {
    // 实现Modal弹窗
    console.log('显示警告弹窗:', options);
  }
  
  // 显示全屏警告
  showFullScreenWarning(options) {
    // 实现全屏警告
    console.log('显示全屏警告:', options);
  }
  
  // 隐藏所有警告
  hideAllWarnings() {
    // 关闭所有警告UI
  }
  
  // 设置视频帧率
  setVideoFPS(fps) {
    console.log(`设置视频帧率: ${fps} fps`);
    // 更新视频发送频率
  }
  
  // 开始录音
  startRecording() {
    console.log('开始录音');
    // 实现录音逻辑
  }
  
  // 播放警报声音
  playAlertSound() {
    const audio = new Audio('/assets/alert.mp3');
    audio.play();
  }
  
  // 挂断通话
  hangup() {
    console.log('挂断通话');
    // 实现挂断逻辑
  }
  
  // 一键报警
  callPolice() {
    console.log('一键报警');
    // 实现报警逻辑
  }
  
  // 自动挂断倒计时
  showAutoHangupCountdown(seconds) {
    console.log(`${seconds}秒后自动挂断`);
    // 实现倒计时UI
  }
}
```

---

### 2. 处理检测结果消息

```javascript
class DetectionResultHandler {
  handleInfoMessage(message) {
    const data = message.data;
    const displayMode = data.display_mode;
    
    switch (displayMode) {
      case 'toast':
        // 轻量提示（低危/安全）
        this.showToast(data.title, data.message);
        break;
        
      case 'popup':
        // 弹窗警告（中危/高危）
        this.showPopup(data.title, data.message, data.risk_level);
        break;
        
      case 'fullscreen':
        // 全屏警告（极危）
        this.showFullscreen(data.title, data.message, data.risk_level);
        break;
    }
  }
  
  showToast(title, message) {
    // 实现Toast提示
    console.log(`Toast: ${title} - ${message}`);
  }
  
  showPopup(title, message, riskLevel) {
    // 实现弹窗
    console.log(`Popup: ${title} - ${message} (${riskLevel})`);
  }
  
  showFullscreen(title, message, riskLevel) {
    // 实现全屏警告
    console.log(`Fullscreen: ${title} - ${message} (${riskLevel})`);
  }
}
```

---

### 3. 完整集成示例

```javascript
class RealtimeDetectionWithDefense {
  constructor(userId, callId, token) {
    this.userId = userId;
    this.callId = callId;
    this.token = token;
    this.ws = null;
    
    // 初始化防御管理器
    this.defenseManager = new DefenseManager();
    this.resultHandler = new DetectionResultHandler();
  }
  
  connect() {
    const url = `ws://localhost:8000/api/detection/ws/${this.userId}/${this.callId}?token=${this.token}`;
    this.ws = new WebSocket(url);
    
    this.ws.onopen = () => {
      console.log('✅ WebSocket连接成功');
      this.startDetection();
    };
    
    this.ws.onmessage = (event) => {
      const message = JSON.parse(event.data);
      this.handleMessage(message);
    };
  }
  
  handleMessage(message) {
    switch (message.type) {
      case 'ack':
        console.log(`✅ ACK: ${message.msg_type}`);
        break;
        
      case 'control':
        // 处理防御升级指令
        this.defenseManager.handleControlMessage(message);
        break;
        
      case 'info':
        // 处理检测结果消息
        this.resultHandler.handleInfoMessage(message);
        break;
        
      case 'detection_result':
        // 处理详细检测结果
        console.log('检测结果:', message);
        break;
        
      default:
        console.log('未知消息:', message);
    }
  }
  
  startDetection() {
    // 开始音视频检测
    // ... 实现代码
  }
}

// 使用示例
const detection = new RealtimeDetectionWithDefense(1, 123, 'your_jwt_token');
detection.connect();
```

---

## 🎨 UI设计建议

### Level 1: 正常模式

```
┌─────────────────────────────────┐
│  通话中...                       │
│  ┌─────┐                         │
│  │ 🟢  │ 安全                    │
│  └─────┘                         │
│                                  │
│  [静音] [免提] [挂断]            │
└─────────────────────────────────┘
```

---

### Level 2: 警惕模式

```
┌─────────────────────────────────┐
│  通话中...                       │
│  ┌─────┐                         │
│  │ 🟡  │ 警惕                    │
│  └─────┘                         │
│                                  │
│  ┌─────────────────────────┐    │
│  │ ⚠️ 风险警告              │    │
│  │                          │    │
│  │ 检测到AI合成语音         │    │
│  │ 置信度: 95%              │    │
│  │                          │    │
│  │ [继续通话] [立即挂断]    │    │
│  └─────────────────────────┘    │
│                                  │
│  [静音] [免提] [挂断]            │
└─────────────────────────────────┘
```

---

### Level 3: 危险模式

```
┌─────────────────────────────────┐
│ 🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨 │
│                                  │
│        🚨 高危风险警告            │
│                                  │
│   检测到AI合成语音 + AI换脸      │
│   + 诈骗话术                     │
│                                  │
│   系统检测到多重风险             │
│   强烈建议立即挂断！             │
│                                  │
│   已通知家人                     │
│   正在录音保存证据               │
│                                  │
│   ┌─────────────────────┐       │
│   │   立即挂断           │       │
│   └─────────────────────┘       │
│                                  │
│   ┌─────────────────────┐       │
│   │   一键报警           │       │
│   └─────────────────────┘       │
│                                  │
│ 🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨 │
└─────────────────────────────────┘
```

---

## 🔄 防御等级切换流程

```
用户开始通话
    ↓
Level 1（正常模式）
    ↓
检测到AI合成语音
    ↓
收到 control 消息: target_level = 2
    ↓
Level 2（警惕模式）
- 显示黄色警告
- 提高检测频率
- 发送短信给家人
    ↓
继续检测到AI换脸
    ↓
收到 control 消息: target_level = 3
    ↓
Level 3（危险模式）
- 显示全屏红色警告
- 震动+声音警报
- 开启录音
- 建议立即挂断
    ↓
用户挂断通话
    ↓
恢复 Level 1
```

---

## ⚠️ 注意事项

### 1. 防御等级只升不降

在同一通话中，防御等级只会升级，不会降级。即使后续检测结果正常，也会保持当前等级直到通话结束。

```javascript
// ❌ 错误：不要降级
if (targetLevel < this.currentLevel) {
  return;  // 忽略降级指令
}

// ✅ 正确：只升级
if (targetLevel > this.currentLevel) {
  this.currentLevel = targetLevel;
  this.applyLevel(targetLevel);
}
```

---

### 2. 多重警告叠加

可能同时收到多个检测结果消息，前端应该合并显示：

```javascript
// 合并多个风险提示
const risks = ['AI合成语音', 'AI换脸', '诈骗话术'];
const message = `检测到: ${risks.join(' + ')}`;
```

---

### 3. 用户体验优化

- Level 2 警告应该允许用户关闭（但保持警惕状态）
- Level 3 警告不应该允许关闭（强制显示）
- 震动和声音应该可以在设置中关闭
- 提供"误报反馈"按钮

---

### 4. 性能考虑

- Level 2 提高检测频率会增加流量，注意提示用户
- 录音功能会占用存储空间，定期清理
- 全屏警告会遮挡通话界面，确保用户仍能操作

---

## 📞 技术支持

如有问题，请联系后端开发团队。

**后端接口文档：** http://localhost:8000/docs

**项目地址：** E:\wangtiao\AI-Anti-Fraud-Detection-System-API

---

## 📝 更新日志

- **2026-02-23**: 初始版本，三层防御机制说明

